package response

import "{{ .Title }}/model"

type Ping struct {
	ID  int    `json:"id"`
	Msg string `json:"msg"`
}

func BuildPing(ping model.Ping) Ping {
	return Ping{
		ID:  ping.ID,
		Msg: ping.Msg,
	}
}

