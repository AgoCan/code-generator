package response

import "{{ .Title }}/internal/pkg/database"

type Ping struct {
	ID  int    `json:"id"`
	Msg string `json:"msg"`
}

func BuildPing(ping database.Ping) Ping {
	return Ping{
		ID:  ping.ID,
		Msg: ping.Msg,
	}
}

